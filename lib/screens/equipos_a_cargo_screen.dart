import 'package:flutter/material.dart';

class EquiposACargoScreen extends StatelessWidget {
  const EquiposACargoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Equipos a Cargo", style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
